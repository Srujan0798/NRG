# Agent Connector Reference

This repo-local reference keeps imported `.agents/skills/*/SKILL.md` connector
links loadable during handover validation. It does not grant connector access;
available connectors are supplied by the active agent runtime.
