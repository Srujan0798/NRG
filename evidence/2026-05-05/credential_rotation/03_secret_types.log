# Secret Types Found in History

Date: 2026-05-05
Scanner: scan_env_history_secrets.py
Result: PASS — 0 findings

## Summary

The local git history scan found **0 secret-like assignments** across all runtime `.env` files.

This means:
- No `.env`, `.env.dev`, `.env.local`, `.env.prod`, or `.env.staging` files exist in git history
- The history rewrite successfully removed all runtime environment files
- The purge from the previous remediation run is verified

## Secret Classes (from previous remediation context)

Based on the `ENV_HISTORY_SECRET_REMEDIATION_2026-05-02.md` runbook, the previous scan found:
- Database credentials (POSTGRES_PASSWORD, DATABASE_URL)
- JWT secrets (JWT_SECRET, JWT_SECRET_KEY)
- Redis credentials (REDIS_PASSWORD, REDIS_URL)
- Model API keys (GEMINI_API_KEY, OPENAI_API_KEY, ANTHROPIC_API_KEY)
- Acceptance user passwords (RESEARCHER_PASSWORD, GOV_PASSWORD, INDUSTRY_PASSWORD)
- Audit chain keys (AUDIT_CHAIN_KEY)
- PII encryption keys (NRG_PII_ENCRYPTION_KEY)

**Current status: All 0 in current history scan**

## Scanner Limitation

The `--all-refs` flag is not supported by this scanner version. The scanner scans all branches and commits by default via `git log --all --full-history`.