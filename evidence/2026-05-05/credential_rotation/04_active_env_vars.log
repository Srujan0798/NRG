# Active Environment Variables Audit

Date: 2026-05-05
Scope: .env*, .yml, .yaml files in working directory

## Tracked Files (gitignore-compliant example templates)

### .env.example
- DATABASE_URL: redacted local dev placeholder
- JWT_SECRET: placeholder, must be replaced
- AUDIT_CHAIN_KEY: redacted dev placeholder

### .env.dev.example
- POSTGRES_PASSWORD: redacted dev placeholder
- JWT_SECRET: redacted dev placeholder
- GEMINI_API_KEY: placeholder

### .env.staging.example / .env.prod.example
- POSTGRES_PASSWORD: CHANGE_ME placeholder, must be set pre-deploy
- JWT_SECRET_KEY: CHANGE_ME placeholder
- GEMINI_API_KEY: CHANGE_ME placeholder

## Untracked Runtime Files (on disk, not in git)

### .env (DEV)
- JWT_SECRET: redacted dev value
- MINIMAX_API_KEY: redacted real-looking value
- NVIDIA_API_KEY: redacted real-looking value
- RESEARCHER_PASSWORD: redacted dev value
- GOV_PASSWORD: redacted dev value
- INDUSTRY_PASSWORD: redacted dev value

### .env.dev
- POSTGRES_PASSWORD: redacted dev value
- JWT_SECRET: redacted dev value
- GEMINI_API_KEY: placeholder

### .env.local
- POSTGRES_PASSWORD: redacted dev value
- JWT_SECRET: redacted dev value

### .env.staging
- POSTGRES_PASSWORD: CHANGE_ME placeholder
- JWT_SECRET_KEY: CHANGE_ME placeholder
- REDIS_PASSWORD: CHANGE_ME placeholder

### .env.prod (NOT DEPLOYED — placeholder template)
- POSTGRES_PASSWORD: CHANGE_ME placeholder
- GEMINI_API_KEY: CHANGE_ME placeholder

## Docker Compose Config

docker-compose.yml references:
- `${POSTGRES_PASSWORD}` — pulled from env vars, not hardcoded
- `${DATABASE_URL}` — pulled from env vars

## Key Findings

1. **LIVE API keys found in `.env`**:
   - MINIMAX_API_KEY (redacted real-looking value)
   - NVIDIA_API_KEY (redacted real-looking value)

2. **Acceptance passwords in `.env`**:
   - RESEARCHER_PASSWORD
   - GOV_PASSWORD
   - INDUSTRY_PASSWORD

3. **PostgreSQL passwords**:
   - `.env.dev`: redacted dev value
   - `.env.local`: redacted dev value

4. **JWT secrets**:
   - All redacted; local evidence indicates dev-pattern values

## Conclusion

The `.env` file contains what appear to be real API keys (MINIMAX, NVIDIA) that are NOT placeholders. These are LIVE credentials that would require rotation if they were ever exposed in git history.

**VERDICT**: Cannot confirm from local scan alone whether these specific keys were in the 286 purged secrets. External verification required.
