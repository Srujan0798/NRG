# Blockers - Killer Queries Live Local API

No Assignment 3 blockers remain for the local live API gate.

All three killer queries pass against `http://localhost:8000`, each returns at least one row, generated SQL is captured, KILLER-02 and KILLER-03 must-contain rules pass, and measured local P95 latency is below 4000 ms.

Remaining broader project blockers are outside Assignment 3:

- C4 P99 is still above the 500 ms gate.
- Staging/deployed URL proof is still not present in `.claude/CURRENT_STATE.md`.
