apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nrg-api-monitor
  namespace: {{ .Values.namespace | default "nrg-production" }}
  labels:
    app: nrg-api
    release: prometheus
spec:
  selector:
    matchLabels:
      app: nrg-api
  endpoints:
    - port: http
      path: /metrics
      interval: {{ .Values.prometheus.metricsScrapeInterval | default "60s" }}
    - port: http
      path: /health
      interval: {{ .Values.prometheus.healthScrapeInterval | default "15s" }}
  namespaceSelector:
    matchNames:
      - {{ .Values.namespace | default "nrg-production" }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nrg-postgres-monitor
  namespace: {{ .Values.namespace | default "nrg-production" }}
  labels:
    app: nrg-postgres
    release: prometheus
spec:
  selector:
    matchLabels:
      app: nrg-postgres
  endpoints:
    - port: tcp-postgres
      path: /metrics
      interval: {{ .Values.prometheus.metricsScrapeInterval | default "60s" }}
  namespaceSelector:
    matchNames:
      - {{ .Values.namespace | default "nrg-production" }}
---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: nrg-api-pod-monitor
  namespace: {{ .Values.namespace | default "nrg-production" }}
  labels:
    app: nrg-api
    release: prometheus
spec:
  selector:
    matchLabels:
      app: nrg-api
  podMetricsEndpoints:
    - port: http
      path: /metrics
      interval: {{ .Values.prometheus.metricsScrapeInterval | default "60s" }}
    - port: http
      path: /health
      interval: {{ .Values.prometheus.healthScrapeInterval | default "15s" }}
  namespaceSelector:
    matchNames:
      - {{ .Values.namespace | default "nrg-production" }}
