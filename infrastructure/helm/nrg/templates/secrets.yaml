{{/*
NRG Secret for database password (placeholder — actual value from Vault)
*/}}
apiVersion: v1
kind: Secret
metadata:
  name: nrg-postgres-secret
  namespace: {{ .Values.namespace | default "nrg-production" }}
  labels:
    app: nrg
    nrg.gov.in/secrets-source: vault
type: Opaque
{{/*
This is a SEALED placeholder. The actual password is fetched from Vault
by the Vault Agent sidecar and written to /vault/secrets/db_password.
This Secret resource is never populated with the actual secret value in Git.
*/}}
stringData:
  password: ""  # Placeholder — actual value in Vault
---
apiVersion: v1
kind: Secret
metadata:
  name: nrg-langfuse-secret
  namespace: {{ .Values.namespace | default "nrg-production" }}
  labels:
    app: nrg-langfuse
    nrg.gov.in/secrets-source: vault
type: Opaque
stringData:
  database-url: ""  # Placeholder
  secret-key: ""     # Placeholder
  nextauth-secret: "" # Placeholder