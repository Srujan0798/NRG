# Connector Reference

This repo-local placeholder exists so imported skills that link to
`../../CONNECTORS.md` remain loadable in NRG. Connector availability is defined
by the active Codex session, not by this file.

For NRG execution, prefer repo-local files, MCP resources explicitly provided in
the session, and the source-of-truth map before relying on external connectors.
