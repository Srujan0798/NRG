# NRG — FINAL DISPATCH (2026-04-28)

**Purpose:** close the remaining Kimi-audit blockers + the 6 commercial gates so the next external review scores ≥ 8.5/10 and IIT-GN / IndiaAI / industry can transfer ₹50L–₹1Cr against a real artefact.

**Source of blockers:** `NRG_FINAL_ETERNAL_AUDIT_2026-04-27.md` (Kimi Code CLI principal-engineer review).

**Target tag:** `v1.0.1-eternal` (replaces the premature `v1.0.0-eternal`).

---

## Wave plan (run in this order; parallel within a wave)

| Wave | Protocols | Duration | Owner |
|---|---|---|---|
| **W1** parallel | DISPATCH-7 (scoped commit plan, no blanket add) · DISPATCH-3 (TRL view verification) · DISPATCH-5 (vocab gate closure) | 2–4 h | backend + database + writer |
| **W2** parallel | DISPATCH-1 (Qdrant zero-vector guard) · DISPATCH-2 (100u load bottleneck) | 6–8 h | ml + devops + backend |
| **W3** parallel | DISPATCH-4 (SSE e2e + cold latency) · DISPATCH-6 (GPG sigs) | 1 day | backend + frontend + founder |
| **W4** sequential | DISPATCH-8 (re-audit + re-tag `v1.0.1-eternal`) | 4 h | founder + Guru |
| **W5** parallel calendar | DISPATCH-9 — C1 entity · C2 IRPC IP letter · C3 CERT-In audit · C5 pricing · C6 use-of-funds · C8 warm intros | 6 weeks | founder + CA + IP lawyer |

---

## DISPATCH-1 — K-1: Qdrant zero-vector guard + drift baseline

```
═══════════════════════════════════════════════════════════════
TASK: Qdrant collection health guard — prevent silent SQL-only fallback
AGENT: ml + devops
PRIORITY: P2-hardening (false alarm converted into monitor guard)
═══════════════════════════════════════════════════════════════

FILES:
  scripts/vector_drift_check.py
  src/skills/rag/skill.py
  src/skills/rag/retriever.py
  src/observability/metrics.py
  infrastructure/monitoring/alert-rules.yml
  infrastructure/cron/nrg-drift-monitor (scheduler)
  evidence/2026-04-28/K1_qdrant_collection_health.json
  evidence/2026-04-28/K1_drift_against_real_vectors.log

PROBLEM:
  The "Qdrant has 0/1800 vectors" claim is stale. Current local
  Qdrant reports 1,800 points in nrg_research. However, if a future
  reindex, restart, or restore leaves the collection with zero points,
  RAG must fail loudly instead of silently falling back to SQL-only mode.

ACTION:
  Phase 1 — FORTIFY: /health must mark retriever status critical when
    points_count / vectors_total is zero. Log exactly
    "CRITICAL: Qdrant collection empty".
  Phase 2 — ELEVATE: expose nrg_qdrant_vectors_total and
    nrg_qdrant_vectors_indexed metrics. Alert on vectors_total == 0.
    Do not alert on indexed_vectors_count == 0 alone: Qdrant can report
    0 indexed vectors for small collections below indexing_threshold
    while still serving 1,800 points.
  Phase 3 — IMMORTALIZE: post-deploy smoke test calls /health and
    refuses GREEN when retriever.status == critical. Re-run drift check
    against real vectors after the guard is merged.

SKILLS TO USE:
  /python-backend · /performance · /security-auditor · /code-review-and-quality

ACCEPTANCE CRITERIA:
  - [ ] /health reports retriever.status == critical when vectors_total == 0
  - [ ] test_health_fails_when_qdrant_empty passes
  - [ ] test_retriever_warns_on_zero_vectors passes
  - [ ] Grafana / Prometheus alert exists for nrg_qdrant_vectors_total == 0
  - [ ] BACKLOG.md K-1 row closed with commit hash

GURU ASSIGNMENT NOTE:
  A false alarm is closed by evidence. A future true empty-collection
  event is closed by monitoring. Treat vectors_total == 0 as a P0
  retrieval outage.

AGENT INSTRUCTIONS:
  - Read .agents/AGENTS.md + shishya_universal.md
  - Read .claude/memory/patterns/live-evidence-requirement.md
  - Read NRG_FINAL_ETERNAL_AUDIT_2026-04-27.md §1 (production risk)
  - Run /pre-commit before commit
  - Commit with [NRG-AUDIT-2026-04-28] qdrant — zero-vector guard

DEPENDS ON: none
═══════════════════════════════════════════════════════════════
```

---

## DISPATCH-2 — K-2: 100-user load test remediation

```
═══════════════════════════════════════════════════════════════
TASK: 100-user load: corrected pacing to < 1% errors, P95 < 2s
AGENT: backend + devops
PRIORITY: P0-blocker (Kimi TRAP 6, Quality Bar C4)
═══════════════════════════════════════════════════════════════

FILES:
  src/orchestration/nodes/synthesizer.py (timeout + cancellation)
  src/data/database_v2.py (asyncpg pool: 20+40)
  src/api/main.py (per-IP + per-user rate limit, gunicorn 4 workers)
  scripts/load_test_100users.py
  tests/load/locustfile.py
  src/cache/redis_cache.py (TTL 300s on /query results)
  evidence/2026-04-28/K2_locust_100u_remediated.csv
  evidence/2026-04-28/K2_locust_100u_remediated.log

PROBLEM:
  The stale 2026-04-26 Locust evidence showed 30 success / 270 errors.
  A 2026-04-28 local rerun with 4 workers and trusted proxy headers
  fixed the 90% error mode under stable pacing: 0.78% query errors and
  P95 1.9s. The corrected >50-QPS pacing run still failed: 11.7%
  query errors and P95 76s, with worker restart during lazy embedding
  model initialization. Cluster activation cannot proceed until that
  bottleneck is fixed and the corrected pacing target passes.

ACTION:
  Phase 1 — FORTIFY: keep 4 API workers and trusted proxy headers.
    Preload or isolate embedding models before the first accepted
    traffic request. Verify no HuggingFace model load occurs inside
    the 5-minute load window.
  Phase 2 — ELEVATE: asyncpg pool size 20 + max_overflow 40; Redis
    result-cache on /query with 300s TTL; slow-query degradation path:
    queries projected > 5s return partial answer + "still searching"
    affordance. Per-IP and per-user rate limits must be active.
  Phase 3 — IMMORTALIZE: locust 100u/5m run becomes a CI nightly job
    that posts P95/P99/error-rate to .github/workflows/nightly.yml;
    fails build if P95 > 2 s or error_rate > 1 %.

SKILLS TO USE:
  /python-backend · /performance · /webapp-testing · /code-review-and-quality

ACCEPTANCE CRITERIA:
  - [ ] locust 100u/5m: P95 < 2s, P99 < 5s, error_rate < 1%
  - [ ] evidence/2026-04-28/locust_100u_v2.json status becomes "pass"
  - [ ] Quality Bar C4 reports PASS against the new CSV (not skipped)
  - [ ] Nightly CI job runs the same load test and posts to a Slack/PD channel
  - [ ] BACKLOG.md K-2 row closed with commit hash

GURU ASSIGNMENT NOTE:
  The corrected-pacing failure is a useful result, not a cosmetic
  failure. It identifies the real hot path: lazy embedding model load
  and worker restart under pressure.

DEPENDS ON: none
═══════════════════════════════════════════════════════════════
```

---

## DISPATCH-3 — K-3: TRL table VIEW alias + 63-byte guard verification

```
═══════════════════════════════════════════════════════════════
TASK: Verify TRL safe view + 63-byte identifier guard
AGENT: backend + database
PRIORITY: P1-hardening (implementation present; verify before tag)
═══════════════════════════════════════════════════════════════

FILES:
  alembic/versions/safe_trl_alias_views_001.py
  db_struct.sql
  src/skills/text_to_sql/validator.py
  tests/config/test_trl_safe_views.py
  tests/skills/test_validator.py

PROBLEM:
  The long TRL source table remains in db_struct.sql as source-of-truth
  schema. The safe path is the database VIEW `trl_stages` plus a
  validator guard that rejects identifiers > 63 bytes before execution.
  This is already implemented locally; the dispatch task is to verify
  it and keep it green.

ACTION:
  Phase 1 — FORTIFY: verify safe_trl_alias_views_001.py creates
    `trl_stages` and `tech_trl_stages` without dropping the source
    table.
  Phase 2 — ELEVATE: verify generated SQL and validator tests use
    `trl_stages`; reject any table, column, or alias identifier over
    63 bytes before SQL execution.
  Phase 3 — IMMORTALIZE: keep the CI/static test that fails if the
    long source table leaks into prompt examples or generated SQL paths.

SKILLS TO USE:
  /database-migrations-sql-migrations · /python-backend · /security-auditor · /code-review-and-quality

ACCEPTANCE CRITERIA:
  - [ ] VIEW trl_stages returns the same row count as the source table
  - [ ] grep -RE 'innovations_at_various_stages' src/skills/text_to_sql
        returns 0 lines outside approved schema-source files
  - [ ] tests/config/test_trl_safe_views.py passes
  - [ ] test_postgres_identifier_over_63_bytes_rejected passes
  - [ ] test_short_trl_view_identifier_passes
  - [ ] CI grep gate green
  - [ ] Dhairya 17/17 + ADV 70/70 still pass against the alias path
  - [ ] BACKLOG.md K-3 row closed with commit hash

GURU ASSIGNMENT NOTE:
  Postgres silently truncates identifiers > 63 bytes. A silent wrong
  answer to a ministry official is a far worse outcome than a 500
  error. K-3 closes the single highest-probability silent-error path
  in the project.

DEPENDS ON: none
═══════════════════════════════════════════════════════════════
```

---

## DISPATCH-4 — K-4: 4-phase streaming SSE for cold queries

```
═══════════════════════════════════════════════════════════════
TASK: Never blank > 200ms — 4-phase progress on cold queries
AGENT: backend + frontend
PRIORITY: P0-blocker (Kimi UX §2 — 7s skeleton "feels broken")
═══════════════════════════════════════════════════════════════

FILES:
  src/api/main.py — /api/query/stream emits SSE events:
    {"phase": "parsing"}      → after sanitiser
    {"phase": "planning"}     → after planner
    {"phase": "querying"}     → after router/executor
    {"phase": "synthesizing"} → after synthesizer
    {"phase": "verifying"}    → after verifier
    {"phase": "answer", "data": {...}} → final
  frontend/src/components/StreamingAnswerPanel.tsx — render named
    phases with milliseconds; never blank > 200 ms; show
    "Analysing across 58 tables…" between events
  frontend/src/components/AnswerPanel/AnswerPanel.tsx — fallback
  evidence/2026-04-28/K4_sse_phases_replay.txt
  evidence/2026-04-28/K4_screenshots/{cold,warm,error}.png

PROBLEM:
  Backend SSE phases and frontend progress components exist, but the
  end-to-end browser path still needs verification and the cold path
  still exceeds the target when the slow synthesis / embedding path is
  hit. In a 90-second judgment window, any blank or unexplained wait is
  a product failure.

ACTION:
  Phase 1 — FORTIFY: verify /api/query/stream emits named phase
    events in order on every query; frontend renders each with a label
    and ms timer. Heartbeat ping every 1s so the connection never blanks.
  Phase 2 — ELEVATE: tie progress bar and "Analyzing across 58 tables"
    copy to the actual phase events; show "still working" at >3s.
    Apply the K-2 embedding preload fix so cold Q05 returns under 3s
    or shows useful progress inside 500ms.
  Phase 3 — IMMORTALIZE: Playwright e2e test asserts each phase label
    appears within budget; latency regression test covers cold Dhairya Q05.

SKILLS TO USE:
  /python-backend · /frontend-react-best-practices · /webapp-testing · /code-review-and-quality

ACCEPTANCE CRITERIA:
  - [ ] /api/query/stream emits 4 phases on every query
  - [ ] StreamingAnswerPanel never blanks > 200 ms (e2e proven)
  - [ ] Cold-query session: every second has visible progress
  - [ ] Screenshots committed to evidence/2026-04-28/K4_screenshots/
  - [ ] BACKLOG.md K-4 row closed with commit hash

GURU ASSIGNMENT NOTE:
  The professor decides in 90 seconds. Seven of those cannot be
  silent. K-4 is the difference between "this feels alive" and
  "did the page freeze".

DEPENDS ON: DISPATCH-2 for latency baseline; can verify UI rendering in parallel
═══════════════════════════════════════════════════════════════
```

---

## DISPATCH-5 — K-5: Vocabulary purge + delivery report rewrite

```
═══════════════════════════════════════════════════════════════
TASK: Rewrite delivery report; rename legacy launch scripts
AGENT: writer + founder
PRIORITY: P0-blocker (own production_only.md rule)
═══════════════════════════════════════════════════════════════

FILES:
  NRG_PRODUCTION_READINESS_REPORT_2026-04-28.md — production
    framing per .claude/rules/production_only.md vocabulary table
  scripts/seed_production_data.py
  scripts/acceptance_verification.py
  scripts/prewarm_acceptance_cache.py
  evidence/2026-04-27/acceptance_verification/
  scripts/forbidden_vocab_check.sh — strengthen to scan committed
    .md headers, not just paths

PROBLEM:
  The production readiness report must use launch and acceptance
  framing throughout. Any reviewer applying our own rule
  (.claude/rules/production_only.md) rejects release-facing language
  that describes the product as anything short of operational.

ACTION:
  Phase 1 — FORTIFY: ensure every legacy path reference in the
    codebase + handover docs is updated.
  Phase 2 — ELEVATE: rewrite the production readiness report. Reframe
    absolute claims as hardening status against the 8 launch
    blockers"; replace the table-row "Score 10/10" with the actual
    Quality Bar 5/6 + Kimi audit findings; add a §0 acknowledging
    the Kimi gaps and the closure plan (this dispatch).
  Phase 3 — IMMORTALIZE: extend forbidden_vocab_check.sh to fail on
    forbidden launch-framing vocabulary anywhere; CI gate is binding.

SKILLS TO USE:
  /docs-sync · /code-review-and-quality

ACCEPTANCE CRITERIA:
  - [ ] forbidden_vocab_check.sh exit 0 across full repo
  - [ ] Renamed scripts referenced consistently across codebase
  - [ ] Production readiness report rewritten with §0 honesty
        paragraph + Kimi gaps acknowledged + closure plan link
  - [ ] BACKLOG.md K-5 row closed with commit hash

GURU ASSIGNMENT NOTE:
  Self-attestation that contradicts your own commissioned audit is
  the fastest path to losing institutional trust. K-5 fixes that
  before any reviewer can use it against you.

DEPENDS ON: nothing — start anytime
═══════════════════════════════════════════════════════════════
```

---

## DISPATCH-6 — K-6: 8 GPG signatures on handover docs

```
═══════════════════════════════════════════════════════════════
TASK: Sign all 8 handover docs; populate signatures/
AGENT: founder
PRIORITY: P0-blocker (handover packet incomplete without these)
═══════════════════════════════════════════════════════════════

FILES (8 docs to sign):
  docs/handover/API_REFERENCE.md
  docs/handover/ARCHITECTURE.md
  docs/handover/DATA_INTAKE_PROTOCOL.md
  docs/handover/OPERATIONS_RUNBOOK.md
  docs/handover/PITCH_DECK_GUIDE.md
  docs/handover/SECURITY_COMPLIANCE_ATTESTATION.md
  docs/handover/SYSTEM_OVERVIEW.md
  docs/handover/UAT_RESULTS.md

OUTPUT:
  docs/handover/signatures/*.asc (8 files)
  docs/handover/signatures/SIGNATURE_MANIFEST.md (refresh with hashes)

PROBLEM:
  signatures/ at repo root is empty. docs/handover/signatures/ has
  only the manifest. The handover packet ships unsigned, which fails
  any procurement review.

ACTION:
  Phase 1 — FORTIFY: verify founder GPG key with
    gpg --list-secret-keys; if absent, generate a 4096-bit RSA key
    and publish pubkey to keys.openpgp.org.
  Phase 2 — ELEVATE: sign each handover doc:
    for f in docs/handover/*.md; do
      gpg --armor --detach-sign --output "docs/handover/signatures/$(basename $f).asc" "$f"
    done
  Phase 3 — IMMORTALIZE: SIGNATURE_MANIFEST.md lists each doc's
    SHA-256 + key fingerprint + signature filename. Add CI job
    "verify-signatures" that runs gpg --verify on every .asc and
    fails build on mismatch.

SKILLS TO USE:
  /security-auditor · /docs-sync

ACCEPTANCE CRITERIA:
  - [ ] 8 .asc files in docs/handover/signatures/
  - [ ] Each .asc verifies clean against founder pubkey
  - [ ] SIGNATURE_MANIFEST.md updated with sha256 + fingerprint
  - [ ] CI verify-signatures job green
  - [ ] BACKLOG.md K-6 row closed with commit hash

DEPENDS ON: K-5 (sign the rewritten docs, not legacy launch wording)
═══════════════════════════════════════════════════════════════
```

---

## DISPATCH-7 — K-7: Scoped commit plan (clean state for tag)

```
═══════════════════════════════════════════════════════════════
TASK: Split dirty tree into reviewed commits; do not blanket add
AGENT: backend (whoever owns the dirty files)
PRIORITY: P0-blocker (gates every "FIXED-AND-VERIFIED" claim)
═══════════════════════════════════════════════════════════════

FILES:
  Review `git status --short`; current tree contains many unrelated
  protocol, docs, infra, backend, frontend, and test changes. Do not
  use `git add -A` until every path is deliberately assigned.

PROBLEM:
  The working tree is too broad for a single WIP commit. A blanket
  commit would mix audit-chain repair, vocabulary cleanup, Qdrant
  health guards, JWT hardening, schema work, docs, and possibly user
  edits. That makes review and rollback worse.

ACTION:
  Phase 1 — FORTIFY: run `git status --short` and assign each path to
    a protocol bucket: K-5, audit-chain, K-1, K-2/K-4, K-3, JWT,
    schema parity, workflow evolution, or user/unrelated.
  Phase 2 — ELEVATE: create scoped commits with explicit path lists
    only. Each commit body cites the evidence commands that passed.
    Run targeted tests before each commit and full pre-commit before
    a tag.
  Phase 3 — IMMORTALIZE: BACKLOG.md rows receive the commit hashes
    only after the corresponding acceptance criteria pass.

ACCEPTANCE CRITERIA:
  - [ ] git status --short → 0 lines (excluding .pid / .coverage)
  - [ ] git log --oneline shows scoped [NRG-AUDIT-2026-04-28] commits
  - [ ] /pre-commit + /test-suite green on the resulting HEAD

DEPENDS ON: nothing — runs FIRST
═══════════════════════════════════════════════════════════════
```

---

## DISPATCH-8 — Re-audit + re-tag `v1.0.1-eternal`

```
═══════════════════════════════════════════════════════════════
TASK: Independent re-audit on 3 AIs; tag v1.0.1-eternal
AGENT: founder + Guru
PRIORITY: P0-blocker (the artefact you walk in with)
═══════════════════════════════════════════════════════════════

FILES:
  evidence/2026-04-28/NRG_PRODUCTION_AUDIT_2026-04-28.md (NEW —
    11 sections per .claude/rules/audit/protocol.md §7)
  evidence/2026-04-28/external_audit_kimi_2026-04-28.md
  evidence/2026-04-28/external_audit_claude_2026-04-28.md
  evidence/2026-04-28/external_audit_<ai3>_2026-04-28.md
  signatures/v1.0.1-eternal.asc
  BACKLOG.md (Sprint line → "v1.0.1-eternal SEALED 2026-04-28")

ACTION:
  Phase 1 — FORTIFY: run scripts/quality_bar_scorecard.py against
    the post-K1..K4 stack. Must read 6/6 (C4 now real, C5 now real).
  Phase 2 — ELEVATE: send the audit_protocol §13 prompt to 3
    different AIs (Kimi, Claude, GPT/Gemini). Attach the 4 required
    files. All 3 must score ≥ 8.5/10. Union of remaining gaps =
    next sprint backlog.
  Phase 3 — IMMORTALIZE: founder GPG-signs
    NRG_PRODUCTION_AUDIT_2026-04-28.md;
    git tag -s v1.0.1-eternal -m "Kimi blockers closed, real eternal"
    git push origin main v1.0.1-eternal
    BACKLOG.md sprint line updated.

ACCEPTANCE CRITERIA:
  - [ ] Quality Bar 6/6 against the running stack
  - [ ] 3 external AI scores all ≥ 8.5/10
  - [ ] git tag -l shows v1.0.1-eternal
  - [ ] signatures/v1.0.1-eternal.asc verifies
  - [ ] BACKLOG.md updated

GURU ASSIGNMENT NOTE:
  v1.0.1-eternal supersedes v1.0.0-eternal. Don't delete the old
  tag — keep it as a record of the premature seal so the team
  remembers why audit discipline matters.

DEPENDS ON: DISPATCH-1, -2, -3, -4, -5, -6
═══════════════════════════════════════════════════════════════
```

---

## DISPATCH-9 — Commercial sprint (parallel to engineering)

```
═══════════════════════════════════════════════════════════════
TASK: 6 commercial gates that turn the artefact into ₹50L–₹1Cr
AGENT: founder + CA + IP lawyer + IRPC liaison + auditor
PRIORITY: P0-blocker for cash transfer
═══════════════════════════════════════════════════════════════

C1 — Pvt Ltd / LLP entity + GST + current account
  Owner: founder + CA
  Week 1; cost ₹15–20K
  Output: CIN/LLPIN, GSTIN, current-account number on letterhead
  Gate: no grantor or buyer transfers to a personal account

C2 — IIT-GN IRPC IP-assignment letter
  Owner: founder + faculty mentor + IRPC
  Week 2-3
  Draft template: "Founder retains commercial rights to NRG codebase
    and trademarks; institute holds non-commercial academic license
    for research and teaching."
  Output: letter signed by IRPC head, on IIT-GN letterhead
  Gate: every buyer's lawyer asks for this; no letter = no deal

C3 — External CERT-In / DPDP empanelled audit
  Owner: founder + auditor (DSCI list — Lucideus, NII, Sectona, AAA)
  Week 3-6; cost ₹3–6L
  Scope: DPDP Act 2023, OWASP Top 10, audit-chain integrity, secrets
    handling, RBAC enforcement at SQL + response shape, red-team RT-01..RT-30
  Output: third-party attestation letter on auditor letterhead
  Gate: government and PSU procurement reject self-attestation

C5 — Pricing doc (3 tiers)
  Owner: founder
  Week 1; output PDF
  Tiers: Institute ₹15L/yr · Ministry ₹25L/yr · Enterprise ₹40L/yr
  Each tier specifies: seats, queries/month, support SLA, training
    pairs included, data residency, audit-chain depth.

C6 — Cap table + 12-month use-of-funds for ₹50L–₹1Cr
  Owner: founder + CA
  Week 1-2
  Line items (illustrative for ₹50L):
    Salary (founder + 2 engineers, 12 mo)   ₹24L
    Cluster (Indian-soil bare-metal)        ₹6L
    External CERT-In/DPDP audit             ₹5L
    GTM (events, IRPC, IndiaAI)              ₹3L
    Legal + IP filings                      ₹2L
    Insurance + compliance                  ₹2L
    Buffer (~16%)                           ₹8L

C8 — 3 warm intros (P1 IRPC seed · P2 MeitY/IndiaAI grant · P3 industry POC)
  Owner: founder
  Continuous; calendar invites confirmed
  Pitch packet: 8-slide deck (problem · sovereign moat · evidence ·
    traction · ask · milestones · team · use-of-funds)

ACCEPTANCE CRITERIA:
  - [ ] CIN/LLPIN issued; GST + current account live
  - [ ] IRPC-signed IP letter in PDF
  - [ ] CERT-In auditor attestation letter in PDF
  - [ ] Pricing PDF, founder-signed
  - [ ] Cap table + use-of-funds spreadsheet, CA-stamped
  - [ ] 3 warm-intro calendar invites confirmed

DEPENDS ON: nothing — start week 1, parallel to engineering
═══════════════════════════════════════════════════════════════
```

---

## Done-checklist (the moment college can transfer ₹50L–₹1Cr)

```
Engineering (post-K1..K4):
☐ /health → retriever.vectors_total > 1500 and retriever.status != critical
☐ Locust 100u: P95 < 2s, error_rate < 1%
☐ Locust 1000u (cluster): P95 < 2s, P99 < 5s
☐ TRL view alias used everywhere; 63-byte guard merged
☐ /api/query/stream emits 4 phases; never blanks > 200 ms
☐ scripts/forbidden_vocab_check.sh exit 0 across repo
☐ 8 docs/handover/signatures/*.asc verified
☐ Quality Bar 6/6 against running stack
☐ 3 external AI scores all ≥ 8.5/10
☐ git tag v1.0.1-eternal pushed; v1.0.0-eternal kept as history

Commercial (post-Wave 5):
☐ CIN/LLPIN issued; GST + current account live
☐ IRPC IP-assignment letter signed
☐ CERT-In/DPDP empanelled audit attestation in PDF
☐ Pricing PDF (3 tiers) founder-signed
☐ Cap table + 12-mo use-of-funds CA-stamped
☐ 3 warm intros booked (P1 IRPC · P2 MeitY/IndiaAI · P3 industry)

Handover packet:
☐ nrg-handover-2026-04-28.zip assembled per MASTER_CLOSURE §6
☐ Founder-signed, SHA-256 manifest, ready for in-person delivery
```

When every box is true, walk into IRPC / IndiaAI / industry with the artefact. The probability of closing ≥ ₹50L within 90 days at this discipline is ~75 %.
